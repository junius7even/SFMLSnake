set(prefix headers)

set(game_headers
        ${prefix}apple.h
        ${prefix}Engine.h
        ${prefix}Wall.h
        ${prefix}SnakeSection.h
        )

set(header_list
        ${game_headers}
        )